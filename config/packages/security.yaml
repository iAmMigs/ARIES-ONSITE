security:
    password_hashers:
        Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface: 'auto'

    providers:
        # This is the CRITICAL part. It tells Symfony to check the 'admin_user' table.
        app_admin_provider:
            entity:
                class: App\Entity\AdminUser
                property: email

    firewalls:
        dev:
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            # This must match the provider name above
            provider: app_admin_provider
            form_login:
                login_path: app_auth_login
                check_path: app_auth_login
                enable_csrf: true
            logout:
                path: app_logout
                # Where to go after logout
                target: app_home

    access_control:
        # Secure the admin URLs so only actual Admins can enter
        - { path: ^/alabang-admin, roles: ROLE_ADMIN }
        - { path: ^/diliman-admin, roles: ROLE_ADMIN }